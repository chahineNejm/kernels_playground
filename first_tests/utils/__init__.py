# first_tests/utils — utility package for kernel‑operator playground
#
# Quick start:
#   from utils.data import load_gift_dataset, quick_peek, dataset_summary
#   from utils.kernels import fit_kernel_operator, predict_kernel_operator
#   from utils.viz import plot_predictions, plot_series_grid
#   from utils.config import DEFAULT_CONFIG
#
# Sub‑modules are imported on demand — no eager imports so that missing
# optional deps (e.g. `datasets`) don't break the whole package.
